<UserControl x:Class="SmartFileOrganizer.Views.OrganizeFilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartFileOrganizer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Folder Selection" Grid.Row="0" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Source Folder:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Text="{Binding SourceFolder}" Grid.Row="0" Grid.Column="1" IsReadOnly="True"/>
                <Button Content="Browse" Grid.Row="0" Grid.Column="2" Margin="10,0,0,0" Command="{Binding SelectSourceFolderCommand}"/>

                <TextBlock Text="Destination Folder:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,10,0"/>
                <TextBox Text="{Binding DestinationFolder}" Grid.Row="1" Grid.Column="1" IsReadOnly="True" Margin="0,10,0,0"/>
                <Button Content="Browse" Grid.Row="1" Grid.Column="2" Margin="10,10,0,0" Command="{Binding SelectDestinationFolderCommand}"/>
            </Grid>
        </GroupBox>

        <CheckBox Content="Include subfolders" IsChecked="{Binding IncludeSubfolders}" Grid.Row="1" Margin="0,0,0,10"/>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,10">
            <Button Content="Organize Files" Command="{Binding OrganizeFilesCommand}" IsEnabled="{Binding IsProcessing, Converter={StaticResource BooleanInverter}}"/>
            <Button Content="Cancel" Command="{Binding CancelOrganizationCommand}" IsEnabled="{Binding IsProcessing}" Margin="10,0,0,0"/>
        </StackPanel>

        <ProgressBar Value="{Binding Progress}" Grid.Row="3" Height="20" Margin="0,0,0,10" Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <TextBlock Text="{Binding CurrentFile}" Grid.Row="4" Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl>
<UserControl x:Class="SmartFileOrganizer.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartFileOrganizer.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer>
        <StackPanel Margin="20">
            <TextBlock Text="Settings Configuration" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            
            <GroupBox Header="General Settings" Margin="0,0,0,20">
                <StackPanel>
                    <CheckBox Content="Enable Dark Theme" IsChecked="{Binding IsDarkThemeEnabled}" Margin="0,0,0,10"/>
                    <CheckBox Content="Show Preview Before Execution" IsChecked="{Binding ShowPreview}" Margin="0,0,0,10"/>
                    <CheckBox Content="Auto-Overwrite Existing Files" IsChecked="{Binding AutoOverwrite}" Margin="0,0,0,10"/>
                    <TextBlock Text="Default Operation:" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding OperationTypes}" SelectedItem="{Binding DefaultOperation}" Margin="0,0,0,10"/>
                    <Button Content="Save Settings" Command="{Binding SaveSettingsCommand}" Style="{StaticResource MaterialDesignRaisedButton}" HorizontalAlignment="Left"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
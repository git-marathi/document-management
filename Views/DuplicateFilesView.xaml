<UserControl x:Class="SmartFileOrganizer.Views.DuplicateFilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartFileOrganizer.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer>
        <StackPanel Margin="20">
            <TextBlock Text="Duplicate Files Manager" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            
            <GroupBox Header="Source Folder" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding SourceFolder}" Grid.Column="0" IsReadOnly="True" Margin="0,0,10,0"/>
                    <Button Content="Browse" Grid.Column="1" Command="{Binding SelectSourceFolderCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
                </Grid>
            </GroupBox>
            
            <Button Content="Scan for Duplicates" Command="{Binding ScanForDuplicatesCommand}" IsEnabled="{Binding IsScanning, Converter={StaticResource BooleanInverter}}" Style="{StaticResource MaterialDesignRaisedButton}" HorizontalAlignment="Left" Margin="0,0,0,20"/>
            
            <TextBlock Text="{Binding StatusMessage}" Margin="0,0,0,20"/>
            
            <GroupBox Header="Duplicate Groups" Margin="0,0,0,20">
                <ListView ItemsSource="{Binding DuplicateGroups}" BorderThickness="0">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1" Padding="10">
                                <StackPanel>
                                    <TextBlock Text="{Binding Files[0].Name}" FontWeight="Bold" FontSize="16"/>
                                    <TextBlock Text="{Binding Files.Count, StringFormat='{}Duplicates: {0}'}" Margin="0,5,0,10"/>
                                    <ListView ItemsSource="{Binding Files}" BorderThickness="0" Margin="10,0,0,0">
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding FullPath}" Grid.Column="0" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding Size, StringFormat='Size: {0:N0} bytes'}" Grid.Column="1" VerticalAlignment="Center" Margin="10,0"/>
                                                    <TextBlock Text="{Binding LastModified, StringFormat='Modified: {0:g}'}" Grid.Column="2" VerticalAlignment="Center" Margin="10,0"/>
                                                    <Button Content="Delete" Grid.Column="3" Margin="10,0" Style="{StaticResource MaterialDesignFlatButton}" Foreground="Red"/>
                                                    <Button Content="Rename" Grid.Column="4" Margin="10,0" Style="{StaticResource MaterialDesignFlatButton}"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <Button Content="Delete Newer" Margin="0,0,10,0" Style="{StaticResource MaterialDesignRaisedButton}"/>
                                        <Button Content="Delete Older" Margin="0,0,10,0" Style="{StaticResource MaterialDesignRaisedButton}"/>
                                        <Button Content="Merge" Margin="0,0,10,0" Style="{StaticResource MaterialDesignRaisedButton}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>